name: 'Identity and remove stale branches'

on:
  schedule:
    - cron: '15 1 * * *'
    
permissions:
  issues: write
  contents: write

jobs:
  stale_branches:
    runs-on: ubuntu-latest
    steps:
    - name: Stale Branches
      uses: crs-k/stale-branches@v2.0.1